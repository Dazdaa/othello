"""First have to be able to run flippy's main function in here just to see that its possible.
Then have to edit flippy's main function so that it can run all the other agents, not just getComputerMove.
This could involve using command line arguments for main.py which can then be used as arguments in flippy's main function.
So can add some arguments to flippy's main function.
"""

from flippy import main

# Now have to change this so that can choose opponent on command line argument.
main()

# run by doing py main.py in powershell.
# Use git in windows VSCode by using WSL terminal.
# Order of command line arguments: python3 main.py opponent ...